{
  "name": "panavibunga-admin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dashboard Admin untuk PanaviBunga Store — situs statis (HTML/CSS/JS ES modules), tanpa build step, memakai Firebase Web SDK v10 via CDN (project Firebase sama dengan PanaviBunga Store).",
  "scripts": {
    "start": "npx serve . -l 5173",
    "dev": "npx serve . -l 5173"
  }
}
